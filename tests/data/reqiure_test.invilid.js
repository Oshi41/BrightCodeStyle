function main() {
    const a = require('fs');
}

// name = lib name
let fylesystem = require('fs');
fylesystem = require('fs');
let f = require('f');

// no absolute links
let fs = require('/usr/local/bin/node/fs');
const rules = require('./get_rules.js'); // in foo.js
const actions = require('./get_rules.js'); // in bar.js
const express = require('/usr/lib/node_modules/express');

// prepend z
const locl = require('../data/pkg/util/locl.js')
// with extension
const zlocl = require('../data/pkg/util/locl')

